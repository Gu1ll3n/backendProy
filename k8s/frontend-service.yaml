apiVersion: v1
kind: Service
metadata:
  name: cemedica-frontend-service
spec:
  selector:
    app: cemedica-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
